# DetectionLab v2 Pipeline Dependencies
# Python 3.11+

# Required for atomic_reader.py — parsing Atomic Red Team YAML definitions
pyyaml>=6.0

# Core pipeline uses Python standard library only:
#   json, re, datetime, pathlib, argparse, uuid, sys, math,
#   dataclasses, hashlib, traceback, time

# Optional development tooling (not required for pipeline execution)
# pytest>=7.0          — if migrating replay_harness to pytest
# sigma-cli>=0.9       — for automated Sigma to KQL conversion
